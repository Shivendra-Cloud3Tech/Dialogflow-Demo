package com.bot.Facade.repository;

import com.bot.Facade.model.RegisterComplain;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RegisterComplainRepo extends JpaRepository<RegisterComplain, Integer> {
}
